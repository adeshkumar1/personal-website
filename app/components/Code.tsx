"use client";
import { CopyBlock } from "react-code-blocks";

const Code = () => {
   const code = `
   #include <iostream>;
   using namespace std;
   int main() {
      cout << "Hello, World!";
      return 0;
      }
   `;
   return (
      <></>
   );
};

export default Code;
